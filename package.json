{
  "name": "heart-rate-monitor",
  "version": "1.0.0",
  "description": "Heart Rate and HRV Monitor",
  "scripts": {
    "prestart": "pip3 install flask flask-socketio bleak nest-asyncio numpy scipy",
    "start": "python app.py",
    "build": "mkdir -p dist && cp -r templates dist/ && cp app.py dist/"
  },
  "dependencies": {
    "socket.io-client": "^4.0.1",
    "chart.js": "^4.3.0"
  }
}